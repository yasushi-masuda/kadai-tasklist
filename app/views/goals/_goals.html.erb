<%= form_with(model: goal, local: true) do |f| %>
  <div class='form-group'>
    <%=f.text_area :set, class: 'form-control',row: '3',placeholder: '目標を書こう！' %>
  </div>
  <%= f.submit '設定する',class: 'btn btn-info' %>
<% end %>